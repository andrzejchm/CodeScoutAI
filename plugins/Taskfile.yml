version: '3'

tasks:
  install:
    desc: Install dependencies for plugins module
    cmds:
      - poetry install
  
  lock:
    desc: Update lock file for plugins module
    cmds:
      - poetry lock
